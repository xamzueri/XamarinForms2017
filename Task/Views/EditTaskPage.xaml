<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Task.EditTaskPage" Title="New Task">
	<ContentPage.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<StackLayout Margin="16">
				<Entry Placeholder="Title" />
				<Entry Placeholder="Description" />
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>
					<Label Text="Task Completed" VerticalOptions="Center" />
					<Switch HorizontalOptions="End" Grid.Column="1" />
				</Grid>
			</StackLayout>
			<Button Text="Save" Grid.Row="1" VerticalOptions="End" Margin="0,8" Clicked="Handle_Clicked" />
		</Grid>
	</ContentPage.Content>
</ContentPage>